#pragma once

#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stddef.h>

#define DEBUG_PRINT_CODE
#define DEBUG_TRACE_EXECUTION

#define UINT8_COUNT (UINT8_MAX + 1) // limit on number of locals in scope at once
